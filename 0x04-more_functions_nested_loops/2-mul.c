#include "main.h"
/**
 * mul - multiplies two integers
 * @a: multiplying
 * @b: multiplier
 * when i call mul(5,4) like this
 * Return: the result of mutiplication
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
